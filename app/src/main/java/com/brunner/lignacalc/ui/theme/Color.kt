package com.brunner.lignacalc.ui.theme

import androidx.compose.ui.graphics.Color

// =================== WARM CRAFT PALETTE ===================

// Primary — Tiefes Holzbraun
val WalnutDeep = Color(0xFF2C1810)      // Dunkelster Ton (Text, Headers)
val Walnut = Color(0xFF5D3A1A)          // Primary
val WalnutLight = Color(0xFF8B6B61)     // Heller Akzent
val WalnutPale = Color(0xFFBCAA9A)      // Muted

// Accent — Warmes Gold
val CraftGold = Color(0xFFC17817)       // Hauptakzent (Favoriten, Sterne, Highlights)
val CraftGoldLight = Color(0xFFD4993A)  // Heller
val CraftGoldMuted = Color(0xFFE0C89A)  // Sehr dezent

// Backgrounds — Warmes Creme/Sand
val Cream = Color(0xFFFAF6F1)           // Haupt-Hintergrund
val WarmWhite = Color(0xFFFFFFFF)       // Karten
val Sand = Color(0xFFF5EDE3)            // Icon-Backgrounds, Felder
val SandDark = Color(0xFFE0D5C8)        // Borders, Dividers

// Text
val TextPrimary = Color(0xFF2C1810)     // Haupttext
val TextSecondary = Color(0xFF7A6B5D)   // Sekundärtext
val TextMuted = Color(0xFFA89B8C)       // Placeholder, Labels
val TextOnDark = Color(0xFFFAF6F1)      // Text auf dunklem Hintergrund

// Funktionsfarben
val GreenOk = Color(0xFF4A7C59)         // Gut / Im Bereich (gedämpftes Grün)
val GreenBg = Color(0xFFE8F3EC)         // Grün-Hintergrund
val RedBad = Color(0xFFB54A3F)          // Schlecht / Ausserhalb (warmes Rot)
val RedBg = Color(0xFFF8ECEA)           // Rot-Hintergrund

// Dark Mode
val DarkBg = Color(0xFF1A1510)          // Hintergrund dunkel
val DarkSurface = Color(0xFF2A2218)     // Karten dunkel
val DarkCard = Color(0xFF352A1F)        // Elevated dunkel
val DarkBorder = Color(0xFF3D3228)      // Borders dunkel
